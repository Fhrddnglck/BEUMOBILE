C:\Program Files\Java\jdk-13.0.1\bin -> my-upload-key.store
alias -> my-key-alias
keystore pass : beumobile



build apk with command -> ./gradlew assembleRelease

build.gradle ->
    signingConfigs {
        release {
            if (project.hasProperty('my-upload-key.keystore')) {
                storeFile file(my-upload-key.keystore)
                storePassword beumobile
                keyAlias my-key-alias
                keyPassword beumobile
            }
        }


gradle.properties->
MYAPP_UPLOAD_STORE_FILE=my-upload-key.keystore
MYAPP_UPLOAD_KEY_ALIAS=my-key-alias
MYAPP_UPLOAD_STORE_PASSWORD=beumobile
MYAPP_UPLOAD_KEY_PASSWORD=beumobile

